import React from 'react';
import { useContent } from "../hooks";

const BrowseScreen = () => {
    return (
        <div>
            <h1>Browse Screen</h1>
        </div>
    );
};

export default BrowseScreen;
